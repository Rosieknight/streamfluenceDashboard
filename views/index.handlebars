<div class="container">
	<div class="row">
		<div class="col-xs-12 text-center" id="top-bar">
			<h1> StreamFluence Dashboard </h1>
		</div>
			<div class="row">
			  {{#each influencer_data}}
				<div class="col-xs-12 text-center">
					<p>{{this.id}}  Name: {{this.name}} <br> Followers: {{this.followers}}
					</p>
				</div>	
			  {{/each}}
			</div>
	</div>
</div>

